export const metadata = {
  title: "Our Projects",
  description:
    "Browse the Image Contracting portfolio — kitchens, bathrooms, additions, roofing, and commercial projects across Southern New Hampshire.",
};

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
